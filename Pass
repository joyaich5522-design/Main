-- 1. Create a safe pcall so the script never crashes
local success, err = pcall(function()
    if setclipboard then
        setclipboard("https://sub2unlock.me/DGMa4yk")
    elseif toclipboard then
        toclipboard("https://sub2unlock.me/DGMa4yk")
    elseif set_clipboard then
        set_clipboard("https://sub2unlock.me/DGMa4yk")
    else
        setclipboard("https://sub2unlock.me/DGMa4yk")
    end
end)

-- 2. Send the system notification to the phone screen
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "Free gamepasses",
    Text = "Link copied! Go to your browser and paste for the script.",
    Duration = 5, -- How long the notification stays on screen (seconds)
    Button1 = "OK"
})
